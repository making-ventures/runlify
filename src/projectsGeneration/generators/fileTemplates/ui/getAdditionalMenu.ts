export const uiGetAdditionalMenuTmpl =
  () => `import {MenuElement} from '../uiLib/menu/MenuItem';

const getAdditionalMenu = () => {
  const menuData: MenuElement[] = [];

  return menuData;
};

export default getAdditionalMenu;
`
